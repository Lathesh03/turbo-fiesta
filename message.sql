CREATE TABLE messages (
    id INT AUTO_INCREMENT PRIMARY KEY,
    message TEXT
);
# TABLE
 to create a table named 'messages' with two columns: 'id' and 'message'. The 'id' column is set as the primary key and will auto-increment for each new row. The 'message' column is of type TEXT, which allows for storing longer text messages.